# FastAPI Core
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Database - Use psycopg2 instead of asyncpg
sqlalchemy==2.0.23
psycopg2-binary==2.9.9  # Binary version, no compilation needed
alembic==1.12.1

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
cryptography==41.0.7

# Pydantic - Use compatible versions
pydantic==1.10.13  # Version 1 doesn't need Rust
pydantic-settings==2.1.0
email-validator==2.1.0

# Telegram
telethon==1.34.0

# Async (lightweight)
aiofiles==23.2.1
aiosqlite==0.19.0  # For local SQLite if needed

# Environment
python-dotenv==1.0.0

# Redis
redis==5.0.1
# Remove hiredis (needs compilation)

# Logging
structlog==23.2.0

# Other utilities
requests==2.31.0
httpx==0.25.2
